# Demo Artifacts

`Founder_Demo_Brief_AgentCollect_2026-06-11.docx` is an intentional founder-call brief artifact for this demo.

The preferred location is this `docs/` folder. In this Codex session, moving the existing binary `.docx` was blocked by the filesystem sandbox, so the file is documented in place instead of duplicated.
